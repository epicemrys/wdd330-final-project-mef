// Central place for constants that depend on the current environment

export const BASE_URL = window.location.origin;

// For API keys, I prefer to keep them in a separate config file that is not committed to version control.